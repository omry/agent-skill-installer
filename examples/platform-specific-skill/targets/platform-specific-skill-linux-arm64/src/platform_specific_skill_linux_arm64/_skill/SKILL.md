---
name: platform-specific-skill-linux-arm64
description: Use this example to verify platform-specific selector installs.
version: 0.1.0
---

# Platform-Specific Skill Linux arm64

This payload represents the Linux arm64 artifact. It uses the same installed
layout as every other platform target.

