Package Codex hooks as `type: skill-command` with a validated skill-relative executable and structured arguments; ASI now binds each native hook to that installation's absolute entry point. Legacy packaged `type: command` declarations are rejected explicitly, and the exported `CodexCommandHook` schema is replaced by `CodexSkillCommandHook`.
